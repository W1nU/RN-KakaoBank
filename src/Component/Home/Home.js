import React from "react"
import {
    View,
    SafeAreaView,
    Text,
    StyleSheet
} from "react-native"

const Home = (props) => {

    return(
        <SafeAreaView style={styles.page}>

        </SafeAreaView>
    )
}

const styles = StyleSheet.create({
    page: {
        
    }
})

export default Home;